const a = 1;

function foo(name) {
  const lastName = name;
  return lastName;
}

const x = 200;

foo("denis");

// eslint-disable-next-line no-console
console.log(x, a);

console.log(window);

console.log(window);

const arrow = (y) => y + 1;

arrow(2);

const obj = {
  name: "den",
};

const name = obj.name;
